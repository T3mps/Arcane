; ============================================================================
; AUTHORED LAYOUT SEED -- an ACTUAL --dump-layout capture, edited to prove it.
; ============================================================================
; ArcaneEditor loads this file under --headless (EditorApp::RetargetLayoutIni)
; with io.IniFilename NULL -- never read, never written -- so this is the ONLY
; layout a headless run ever sees, and no run can mutate it. That is what
; makes a headless editor capture reproducible across machines.
;
; PROVENANCE: this is the real output of ImGui's own SaveIniSettingsToDisk,
; captured from a genuine --headless run of ReferenceProject (Task 10,
; plan-b comparator). `--dump-layout` replaces the four manual desk steps
; this file used to prescribe (run windowed, arrange, close, copy the ini)
; with one command that needs no windowed session at all:
;
;   cd bin\Debug-windows-x86_64-md\ArcaneEditor
;   ArcaneEditor.exe --project ReferenceProject --headless --backend dx12 ^
;     --frames 30 --dump-layout dumped-layout.ini
;
; Every [Window] / [Docking][Data] entry below names a window ArcaneEditor
; actually submits (EditorDockHost, Outliner, Inspector, Material, Asset
; Browser, Asset Graph, Asset Status, Console, Problems, Viewport -- plus
; ImGui's own unconditional implicit fallback, Debug##Default, created every
; frame by NewFrame() itself and harmless to pin), with dock-node ids ImGui
; itself emitted -- NOT hand-written, unlike the placeholder this replaces.
; That file's SECOND entry (GoldenImageTest.cpp's "[golden]" case greps for
; its exact spelling and is deliberately not repeated here, to keep that
; regression check meaningful against THIS file too) named a
; dockspace-viewport-flavoured window that does NOT exist in this editor at
; all -- confirmed by dumping a run with NO seed on disk at all and finding
; no such entry -- so LoadIniSettingsFromDisk applied nothing for it and the
; "pinned layout" was really BuildDefaultLayout (EditorPanels.cpp) every run.
; (The FIRST --dump-layout capture taken while authoring THIS file still
; carried that same ghost, verbatim, because the old placeholder was still
; the seed on disk at the time -- ImGui keeps an unclaimed [Window] entry
; from whatever it loaded and re-emits it untouched on save. A second,
; no-seed re-dump is what caught it; this file is built from THAT clean
; capture, not the tainted first one.)
;
; PANEL-SPLIT REGEN (spec s13, Task 9): the Assets panel that used to tab
; alongside Console/Problems in dock node 0x00000006 was split into three
; separate panels -- Asset Browser, Asset Graph, Asset Status -- so the
; committed seed was regenerated the same way, from a clean no-seed
; --headless capture, following the exact TO REGENERATE steps below (the
; PREVIOUS seed was moved aside first so it could not re-emit its own
; "[Window][Assets]" entries into the fresh dump).
;
; DELIBERATELY NOT the bare dump, though. A raw --dump-layout capture off an
; UNSEEDED run is BuildDefaultLayout's own output -- ImGui built it fresh on
; frame 1 because no prior dock state existed to load (EndDockSpace's
; `DockBuilderGetNode(dockspaceId) == nullptr` branch) -- so it would be
; byte-for-byte what a run with NO seed at all also produces. A test that
; asserts "the seed was applied" would pass either way, which is exactly the
; placeholder's failure wearing a passing test. So this file's Inspector dock
; node (id 0x00000004) was widened from BuildDefaultLayout's 231px to 460px,
; with its sibling (id 0x00000003, which carries Viewport + the Asset
; Browser/Asset Graph/Asset Status/Console/Problems tabs) narrowed to 588px
; to compensate (588 + 460 = 1048, their parent's own SizeRef) -- a visible,
; deliberate departure from the built-in default. A run that loads THIS file
; and a run that falls back to BuildDefaultLayout (seed missing, or ignored)
; render different pictures; GoldenImageTest.cpp's with-seed/without-seed
; screenshot comparison is the proof, and the [golden] text case below is
; the cheap, no-GPU guard that this file keeps naming real windows going
; forward.
;
; TO REGENERATE: temporarily move this file aside FIRST (an already-tainted
; seed re-dumps its own unclaimed ghost entries verbatim, as above), run the
; --dump-layout command above, re-apply whatever visible edit distinguishes
; the result from BuildDefaultLayout (widening a different panel is fine --
; the point is ONE deliberate, visible departure), and copy the result over
; this file, KEEPING this header, then commit.
; ============================================================================

[Window][Outliner]
Pos=0,73
Size=230,647
Collapsed=0
DockId=0x00000001,0

[Window][Inspector]
Pos=820,73
Size=460,647
Collapsed=0
DockId=0x00000004,0

[Window][Material]
Collapsed=0
DockId=0x00000004

[Window][Asset Browser]
Pos=232,540
Size=588,180
Collapsed=0
DockId=0x00000006,0

[Window][Asset Graph]
Pos=232,540
Size=588,180
Collapsed=0
DockId=0x00000006,1

[Window][Asset Status]
Pos=232,540
Size=588,180
Collapsed=0
DockId=0x00000006,2

[Window][Console]
Pos=232,540
Size=588,180
Collapsed=0
DockId=0x00000006,3

[Window][Problems]
Pos=232,540
Size=588,180
Collapsed=0
DockId=0x00000006,4

[Window][Viewport]
Pos=232,73
Size=588,465
Collapsed=0
DockId=0x00000005,0

[Window][EditorDockHost]
Pos=0,0
Size=1280,720
Collapsed=0

[Window][Debug##Default]
Pos=60,60
Size=400,400
Collapsed=0

[Docking][Data]
DockSpace       ID=0xCF9D2D5F Window=0x5F6F4589 Pos=0,73 Size=1280,647 Split=X
  DockNode      ID=0x00000001 Parent=0xCF9D2D5F SizeRef=230,720 Selected=0x66BFF854
  DockNode      ID=0x00000002 Parent=0xCF9D2D5F SizeRef=1048,720 Split=X
    DockNode    ID=0x00000003 Parent=0x00000002 SizeRef=588,720 Split=Y
      DockNode  ID=0x00000005 Parent=0x00000003 SizeRef=588,538 CentralNode=1 Selected=0xC450F867
      DockNode  ID=0x00000006 Parent=0x00000003 SizeRef=588,180 Selected=0x36AF052B
    DockNode    ID=0x00000004 Parent=0x00000002 SizeRef=460,720 Selected=0x36DC96AB

[ArcaneEditorLayout][MaterialPanel]
PreviewSplit=0.5500

[EditorPlayMode][State]
Mode=0

[EditorPanels][Visibility]
Outliner=1
Inspector=1
Asset Browser=1
Asset Graph=1
Asset Status=1
Problems=1
Console=1
