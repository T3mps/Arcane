// ArcaneServer: placeholder for the engine-server host. Empty by design --
// the directional rule is that server-side capability gets built IN Arcane
// as engine tooling (never forked server-side), and this is where that host
// will grow. Compiles to prove the project shape; nothing runs yet.
int main()
{
    return 0;
}
