{
  "schemaVersion": 2,
  "note": "Committed suite counts, keyed by name + configuration + INVOCATION. A count is only comparable against a count taken THE SAME WAY, so every entry names the exact test-spec it was measured under and scripts/check-baselines.ps1 matches on all three -- a lane whose -Invocation has no entry here reports 'NO BASELINE -- not checked' instead of passing on slack it never earned. All six entries below are `ArcaneTests.exe \"~[gpu]\"`; `~[gpu]` is a BASELINE-COMPARABILITY convention, not a safety gate -- the [gpu] cases run clean on this hardware. The Jenkins lane runs the suite UNFILTERED, on an agent where [gpu] adds roughly 62000 further assertions, so it deliberately has no baseline here and reports not-checked until one is read off a real Jenkins run and committed. A drop means coverage was lost. Dist legitimately runs fewer cases than Debug/Release (6 cases / 68 assertions fewer) because of pre-existing `#if !defined(ARCANE_DIST)` guards in HostConfigTest.cpp and NriDiagnosticsTest.cpp -- that gap has been constant across this whole arc. The 2026-09-03 rise of +31 assertions / +12 cases in every configuration is Arc B's `[reference]`, `[verify]` and `[witness-unit]` unit cases, which live inside `~[gpu]`; that arc's two `[witness][gpu]` host scenarios sit OUTSIDE this filter by construction and contribute nothing here -- they are counted only by the unfiltered run. The 2026-09-04 rise of +1404 assertions / +97 cases in every configuration is F2b's asset-cook + bindless pipeline unit suites (including the final-review fix wave's 11 further cases: superseded-key removal, multi-candidate client resolution, first-seen cook trigger, case-insensitive meta format, mip-table bounds) (ArtifactReaderTest, AssetPipelineFormatTest/ImporterTest/SessionTest/StoreTest, BindlessTableTest, CookQueueTest, JobSystemSubmitTest, NriTextureCacheArtifactTest, TextureMetaPanelTest, plus additions to SceneRenderResolverTest/ShaderEditorDocumentTest/VendorSmokeTest) -- raw TEST_CASE count in ArcaneTests/src rose 1343 -> 1435 (+92) across the arc, of which 6 are newly [gpu]-tagged (27 -> 33, matching that sweep's own rise) and 86 fall inside ~[gpu], exactly the measured rise. The 2026-09-06 rise of +288 assertions / +33 cases in every configuration is asset-manager Plan 1's three new suites -- AssetReferencesTest (13 cases: MaterialSurfaceFor, ListAssetReferences, the scene structural scan and its extension-coverage pin), AssetPanelModelTest (14: fold/group/search/rail/health semantics and CookStateOf), CreateAssetDialogTest (6: name validation and the CreateAssetRequest bypass invariant) -- plus assertion-only additions to AssetBrowserTest and SceneRenderResolverTest. Raw TEST_CASE count in ArcaneTests/src rose 1475 -> 1508 (+33) across that plan, ALL of them inside ~[gpu]: the [gpu] case count is unchanged at 33 (unfiltered Debug 1495 cases minus ~[gpu] 1462), so this plan added no GPU coverage and none of its rise is invisible to this filter.",
  "measured": "2026-09-06, asset-manager Plan 1 close (Tasks 1-16, 59dd6414..2f5dc391): three-config build 0 warnings / 0 errors, each figure pasted from its own run's final line, `ArcaneTests.exe \"~[gpu]\"` FROM the exe directory -- Debug seed 829257050, Release seed 1685340276, Dist seed 3577943350",
  "baselines": [
    { "name": "arcanetests.assertions", "configuration": "Debug",   "invocation": "~[gpu]", "value": 54270, "unit": "assertions" },
    { "name": "arcanetests.cases",      "configuration": "Debug",   "invocation": "~[gpu]", "value": 1462,  "unit": "cases" },
    { "name": "arcanetests.assertions", "configuration": "Release", "invocation": "~[gpu]", "value": 54270, "unit": "assertions" },
    { "name": "arcanetests.cases",      "configuration": "Release", "invocation": "~[gpu]", "value": 1462,  "unit": "cases" },
    { "name": "arcanetests.assertions", "configuration": "Dist",    "invocation": "~[gpu]", "value": 54202, "unit": "assertions" },
    { "name": "arcanetests.cases",      "configuration": "Dist",    "invocation": "~[gpu]", "value": 1456,  "unit": "cases" }
  ]
}
